# ECDIS Route Planner - 系统架构文档

## 1. 系统概述

ECDIS航线规划系统是一个符合IMO MSC.232(82)标准的海事导航路径规划器，提供自动化路径规划、安全验证和COLREG合规性检查。

### 核心能力
- Hybrid A*路径规划算法
- S-57/S-101海图数据支持
- COLREG国际避碰规则实现
- TSS分道通航制合规
- 实时路径验证与优化

## 2. 系统架构

### 2.1 模块结构

```
lib/
├── planner/              # 路径规划核心
│   ├── hybrid_astar.py   # Hybrid A*算法实现
│   ├── incremental_replan.py # 增量重规划
│   ├── speed_profile.py  # 速度剖面优化
│   └── curvature_xtd.py  # 曲率与XTD计算
│
├── region/               # 可行域管理
│   ├── feasible_region.py # 可航区域构建
│   └── tss_layers.py     # TSS分层管理
│
├── colreg/              # COLREG规则引擎
│   ├── rules.py         # 避碰规则实现
│   └── scenario_runner.py # 场景测试框架
│
├── checks/              # 验证系统
│   └── route_checker.py # 综合路径验证
│
├── enc/                 # 海图数据处理
│   ├── s57_reader.py    # S-57格式读取
│   └── s101_adapter.py  # S-101格式适配
│
├── costs/               # 成本计算
│   └── cost_field.py    # 成本场生成
│
├── traffic/             # 交通管理
│   └── cpa.py          # CPA/TCPA计算
│
└── io/                  # 输入输出
    ├── rtz.py          # RTZ格式支持
    └── s421_export.py  # S-421标准导出
```

### 2.2 核心组件

#### 2.2.1 Hybrid A* 路径规划器
- **算法**: 结合A*搜索与连续运动原语
- **特性**: 
  - 考虑船舶运动学约束
  - 最小转弯半径限制
  - 动态障碍物避让
  - 多分辨率搜索

#### 2.2.2 COLREG规则引擎
- **实现规则**:
  - Rule 7: 碰撞风险评估
  - Rule 8: 避碰行动
  - Rule 9: 狭水道
  - Rule 10: 分道通航制
  - Rule 13-17: 各类遭遇局面
  - Rule 19: 能见度受限

- **核心功能**:
  - CPA/TCPA精确计算
  - 遭遇类型自动识别
  - 多级风险评估
  - 推荐行动生成

#### 2.2.3 可行域构建器
- **数据源**: S-57/S-101海图
- **处理流程**:
  1. 提取危险区域(DEPARE, UWTROC等)
  2. 构建no-go areas
  3. 生成安全水深轮廓
  4. 创建可航多边形

#### 2.2.4 路径验证器
- **验证类别**:
  - 安全性: 水深、障碍物、禁航区
  - TSS合规: 航道方向、分隔带
  - 几何约束: XTD、转弯半径
  - 速度限制: 区域限速、加速度
  - COLREG合规: 避碰规则

## 3. 数据流

```
海图数据(S-57/S-101) → 可行域构建 → Hybrid A*规划 → 路径验证 → 输出(RTZ/S-421)
                          ↑                    ↓
                      交通数据 ←─── COLREG评估
```

## 4. 关键算法

### 4.1 CPA/TCPA计算
```python
# 相对位置(海里)
dx = (lon2 - lon1) * 60 * cos(radians((lat1 + lat2) / 2))
dy = (lat2 - lat1) * 60

# 相对速度
dvx = speed2 * sin(radians(course2)) - speed1 * sin(radians(course1))
dvy = speed2 * cos(radians(course2)) - speed1 * cos(radians(course1))

# TCPA计算
tcpa = -(dx * dvx + dy * dvy) / (dvx^2 + dvy^2)

# CPA距离
cpa = sqrt((dx + dvx * tcpa)^2 + (dy + dvy * tcpa)^2)
```

### 4.2 遭遇类型判定
- **对遇**: 相对方位 < 6°，相反航向
- **追越**: 目标在正横后22.5°以后
- **交叉**: 
  - 右舷让路: 目标在右舷10°-112.5°
  - 左舷直航: 目标在左舷247.5°-350°

## 5. 性能指标

- 路径规划: < 2秒 (100海里航线)
- COLREG评估: < 100ms (单船对)
- 验证检查: < 500ms (完整验证)
- 内存使用: < 500MB

## 6. 标准合规

### IMO标准
- MSC.232(82): ECDIS性能标准
- MSC.252(83): 导航系统测试
- A.694(17): 船载无线电设备

### IHO标准
- S-52: ECDIS显示规范
- S-57: 电子海图数据格式
- S-101: 新一代ENC产品规范
- S-421: 路径规划交换格式

### COLREG
- 1972国际海上避碰规则完整实现

## 7. 接口规范

### 输入接口
- 起止点: WGS84坐标
- 船舶参数: 吃水、长度、宽度、速度
- 海图数据: S-57/S-101格式
- 交通数据: AIS/雷达目标

### 输出接口
- 路径: WGS84航点序列
- 验证报告: JSON格式合规性报告
- 交换格式: RTZ/S-421标准

## 8. 部署架构

```
┌─────────────┐     ┌──────────────┐     ┌────────────┐
│   ECDIS UI  │────▶│ Route Planner│────▶│  Validator │
└─────────────┘     └──────────────┘     └────────────┘
                            │                     │
                    ┌───────▼────────┐   ┌───────▼──────┐
                    │  ENC Database  │   │ COLREG Rules │
                    └────────────────┘   └──────────────┘
```

## 9. 测试架构

- **单元测试**: 各模块独立功能
- **集成测试**: 模块间交互
- **场景测试**: COLREG标准场景
- **性能测试**: 响应时间与资源使用
- **金样测试**: 回归与审批测试

## 10. 安全考虑

- 深度安全边际
- 双重验证机制
- 故障安全设计
- 审计日志记录

---
*文档版本: 2.0*  
*更新日期: 2025-08-10*  
*状态: 生产就绪*